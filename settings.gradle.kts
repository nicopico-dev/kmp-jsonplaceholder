pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}

include(":app", ":shared")
rootProject.name = "KMP POC - JSONPlaceholder API Client"
